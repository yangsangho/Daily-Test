<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/basicBackground</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:fontFamily">@font/font_nanum</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="CalendarTheme" parent="Theme.AppCompat.Light.NoActionBar" />

    <!--    EditText는 전체로 안 먹히네-->
    <style name="EditTextStyle" parent="@android:style/Widget.DeviceDefault.EditText">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:fontFamily">@font/font_nanum</item>
        <item name="android:textSize">@dimen/size_basic</item>
    </style>

    <style name="PopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="AlignAlertDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <!-- gravity 설정하니까 크기에 맞춰서 중간에 오더라 -->
        <item name="windowFixedWidthMajor">@dimen/align_dialog_width_major</item>
        <item name="windowFixedWidthMinor">@dimen/align_dialog_width_minor</item>
        <!-- window ~~ Major : 최대크기, minor : 최소크기   -> %로 비율 지정-->
<!--        근데 minor에 따라 달라지네...??-->
    </style>

    <style name="TabLayoutTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>
</resources>